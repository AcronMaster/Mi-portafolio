// Este archivo fue creado por error. El manifiesto válido es `package.json`.
// Elimina este archivo si no es necesario.

// Nota: mantener este archivo no afecta a npm, que usa package.json.
{
  "name": "mi-portafolio",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.164.0"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  }
}
